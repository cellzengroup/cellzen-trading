# Relative on purpose: the browser calls the dev server it loaded the page from and
# Vite proxies /api to the backend (see vite.config.js). An absolute
# http://localhost:5300/api would point a phone at the phone itself.
VITE_API_URL=/api
